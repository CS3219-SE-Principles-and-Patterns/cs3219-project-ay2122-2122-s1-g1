# PeerPrep

## Client

In `/client` folder,
- Run `npm install`
- Run `npm start`
- Access `http://localhost:3000`

## Server

In `/server` folder,
- Run `npm install`
- Run `npm start`
- Backend endpoints can be accessed at `http://localhost:5000`
